<!DOCTYPE html>
<html lang="pt-br">
<head>
    <% include ../partials/head %>
</head>
<body class="container">
<header>
    <% include ../partials/header %>
</header>
<main>
    <% if (locals.peca) {
        peca = locals.peca;
    } else {
        peca = {};
    } %>
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="autocard">
                <form class="form-signin" method="post" action="<%= !locals.peca ? "/cadastroPeca" : "/editarPeca" %>">
                    <% if (locals.peca) { %>
                    <input type="hidden" name="id" value="<%= locals.peca.id %>">
                    <% } %>
                    <!--<form class="form-signin" method="post" action="/signUp">-->
                    <label for="nome">Nome:</label>
                    <input type="text" class="form-control" placeholder="Nome" name="nome"
                           value="<%= locals.peca ? peca.nome : '' %>" required autofocus>
                    <br>
                    <label for="valor">Valor:</label>
                    <input type="number" class="form-control" placeholder="Preço" name="valor"
                           value="<%= locals.peca ? peca.valor : '' %>" required>
                    <br>
                    <label for="Quantidade em estoque">Quantidade em estoque:</label>
                    <input type="number" class="form-control" placeholder="Quantidade em estoque"
                           name="quantidade_em_estoque" value="<%= locals.peca ? peca.quantidade_em_estoque : '' %>"
                           required>
                    <br>
                    <button class="btn btn-lg btn-primary btn-block" type="submit">
                        <% if (!locals.peca){ %>
                        Cadastrar
                        <% } else { %>
                        Salvar
                        <% } %>
                    </button>
                    <br>

                </form>
                <% if(locals.peca) { %>
                <form action="/excluirPeca" method="post" style="padding: 15px">
                    <input type="hidden" name="id" value="<%= locals.peca.id %>">
                    <button class="btn btn-lg btn-block btn-danger" type="submit">Excluir</button>
                </form>
                <% } %>

                <% if (locals.query && locals.query.err) { %>
                <div>Ocorreu um erro inesperado.</div>
                <% } %>
            </div>
        </div>
    </div>
</main>

</body>
</html>